# Copy to .env and fill in. Used by docker-compose for backend and first-time admin.
# Optional: create first admin and doctor when database is empty (only on first run).
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-password
ADMIN_NAME=Admin
DOCTOR_EMAIL=doctor@example.com
DOCTOR_PASSWORD=doctor-secure-password
DOCTOR_NAME=Doctor

# Backend secrets and APIs (optional for dev)
SECRET_KEY=change-me-in-production-use-long-random-string
GEMINI_API_KEY=
WHISPER_MODEL=tiny
MS_GRAPH_ACCESS_TOKEN=
